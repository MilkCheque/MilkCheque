services:
  db:
    image: mysql:8.0
    container_name: mysql
    environment:
      - MYSQL_USER=${SQL_USER}
      - MYSQL_PASSWORD=${SQL_PASS}
      - MYSQL_ROOT_PASSWORD=ayoy
    ports:
      - 3306:3306
    volumes:
        - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
